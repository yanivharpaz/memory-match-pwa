{
  "name": "memory-match",
  "version": "5.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "start": "npx http-server dist -p 4173 -c-1",
    "test": "npm run build && node tests/compatibility.test.cjs"
  },
  "browserslist": [
    "Safari >= 10",
    "iOS >= 10.3"
  ],
  "devDependencies": {
    "esbuild": "^0.25.10",
    "sharp": "^0.34.4"
  }
}